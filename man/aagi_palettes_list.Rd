% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-aagi_palettes_list.R, R/data.R
\docType{data}
\name{aagi_palettes_list}
\alias{aagi_palettes_list}
\title{AAGI sequential and diverging colour palettes (hex codes)}
\format{
A named list of named lists of character vectors.

A named list of named lists of character vectors.
}
\description{
A nested list of sequential and diverging AAGI colour palettes used by
\code{\link[=aagi_palettes]{aagi_palettes()}}. The top-level names are palette identifiers (e.g.
\code{"aagi_RdTl"}). Each palette is itself a named list keyed by the number of
colour stops (\code{"3"}, \code{"4"}, …). Each entry is a character vector of hex
colour codes of length \code{n}.

A nested list of sequential and diverging AAGI colour palettes used by
\code{\link[=aagi_palettes]{aagi_palettes()}}. The top-level names are palette identifiers (e.g.
\code{"aagi_RdTl"}). Each palette is itself a named list keyed by the number of
colour stops (\code{"3"}, \code{"4"}, …). Each entry is a character vector of hex
colour codes of length \code{n}.
}
\details{
This dataset is intended for internal reuse and for users who want direct
access to the fixed palette definitions without calling the palette function.

Not all palettes support all values of \code{n}. For example, \code{"aagi_RdYl"} is
defined only for 3–7 stops in the source function.

This dataset is intended for internal reuse and for users who want direct
access to the fixed palette definitions without calling the palette function.

Not all palettes support all values of \code{n}. For example, \code{"aagi_RdYl"} is
defined only for 3–7 stops in the source function.
}
\examples{
# List available palettes
names(aagi_palettes_list)

# Available 'n' values for a palette
names(aagi_palettes_list[["aagi_RdTl"]])

# Get the 11-stop RdTl palette
aagi_palettes_list[["aagi_RdTl"]][["11"]]

# Reverse order (equivalent to direction = -1)
rev(aagi_palettes_list[["aagi_RdTl"]][["11"]])

# List available palettes
names(aagi_palettes_list)

# Available 'n' values for a palette
names(aagi_palettes_list[["aagi_RdTl"]])

# Get the 11-stop RdTl palette
aagi_palettes_list[["aagi_RdTl"]][["11"]]

# Reverse order (equivalent to direction = -1)
rev(aagi_palettes_list[["aagi_RdTl"]][["11"]])

}
\keyword{datasets}
